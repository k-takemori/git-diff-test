name: Developt test

on: 
  pull_request:
    branches:
      - 'develop'

jobs:
  develop-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: do test
      run: |
        echo "develop test"
